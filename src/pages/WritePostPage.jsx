import React from 'react';
import { WritePostFormContainer } from '@containers/Form';
import { Layout } from '@components/Layout';

const WritePostPage = (props) => {
  return (
    <Layout>
      <WritePostFormContainer />
    </Layout>
  );
};

export default WritePostPage;
